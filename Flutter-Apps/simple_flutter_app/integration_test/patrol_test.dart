import "package:flutter/material.dart"; import "package:flutter_test/flutter_test.dart"; import "package:integration_test/integration_test.dart"; import "package:simple_flutter_app/main.dart" as app; void main() { IntegrationTestWidgetsFlutterBinding.ensureInitialized(); group("Simple Flutter App Integration Tests", () { testWidgets("App loads and displays all main components", (tester) async { await tester.pumpWidget(const app.MyApp()); await tester.pumpAndSettle(); expect(find.text("Simple Flutter App"), findsOneWidget); expect(find.text("Welcome! 👋"), findsOneWidget); expect(find.text("Interactive Counter"), findsOneWidget); expect(find.text("0"), findsOneWidget); final incrementButton = find.byIcon(Icons.add); if (incrementButton.evaluate().isNotEmpty) { await tester.tap(incrementButton); await tester.pumpAndSettle(); expect(find.text("1"), findsOneWidget); } final decrementButton = find.byIcon(Icons.remove); if (decrementButton.evaluate().isNotEmpty) { await tester.tap(decrementButton); await tester.pumpAndSettle(); expect(find.text("0"), findsOneWidget); } expect(find.text("Platform Information"), findsOneWidget); expect(find.text("Home"), findsOneWidget); expect(find.text("Explore"), findsOneWidget); expect(find.text("Favorites"), findsOneWidget); expect(find.text("Profile"), findsOneWidget); print("✅ All integration tests passed successfully!"); }); }); }
